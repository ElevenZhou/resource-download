#!/usr/bin/env bash
# Launch GoodShort CLI (cases/goodshort) from repo root.
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
export PYTHONPATH="${ROOT}/cases/goodshort${PYTHONPATH:+:$PYTHONPATH}"
exec python3 -m gsdl "$@"
